# .gitignore 配置说明

## 📝 概述

这个 `.gitignore` 文件针对 Whisper 多语言字幕提取项目进行了全面优化，确保只有必要的源代码文件被提交到 Git 仓库，而排除所有生成文件、临时文件和敏感数据。

## 🎯 主要排除类别

### 1. Python 相关
```gitignore
__pycache__/          # Python 字节码缓存
*.py[cod]            # 编译后的 Python 文件
*.egg-info/          # Python 包信息
.pytest_cache/       # 测试缓存
.mypy_cache/         # 类型检查缓存
```

### 2. Whisper 专用文件
```gitignore
# 音频/视频文件目录 - 保留目录结构，排除文件内容
audio_input/*           # 排除输入音频文件
!audio_input/.gitkeep   # 但保留目录说明文件
audio_output/*          # 排除输出字幕文件  
!audio_output/.gitkeep  # 但保留目录说明文件

# 媒体文件扩展名（项目中任何位置）
*.wav, *.mp3, *.mp4     # 各种媒体格式

# 字幕文件
*.srt, *.vtt, *.json    # 生成的字幕文件

# Whisper 模型
whisper_models/         # 下载的模型文件
*.pt, *.bin            # 模型权重文件
```

### 3. 容器化部署
```gitignore
.dockerignore        # Docker 忽略文件
*.kubeconfig        # Kubernetes 配置
secrets/            # 敏感配置目录
```

### 4. 开发环境
```gitignore
venv/, .env          # 虚拟环境
.vscode/, .idea/     # IDE 配置
*.log               # 日志文件
```

## 🔧 特殊配置说明

### 音频/视频文件排除
- **策略**: 保留目录结构，排除文件内容
- **原因**: 媒体文件通常很大（几MB到几GB），不适合存储在 Git 中
- **实现**: 使用 `audio_input/*` 而不是 `audio_input/` 来保留空目录
- **建议**: 使用 Git LFS 或外部存储服务管理大型媒体文件

### 模型文件排除
- **原因**: Whisper 模型文件（39MB - 2.9GB）太大，会拖慢仓库
- **解决方案**: 模型会在运行时自动下载到 `whisper_models/` 目录

### 字幕输出排除
- **原因**: 生成的字幕文件是程序输出，不是源代码
- **例外**: 可以保留示例字幕文件用于测试

## 📁 目录结构规划

### ✅ 应该提交的文件
```
whisper/
├── README.md                    # 项目文档
├── requirements.txt             # 依赖列表
├── pyproject.toml              # 项目配置
├── whisper/                    # 源代码
├── tests/                      # 测试代码
├── deploy.sh                   # 部署脚本
├── batch_subtitle.sh           # 批量处理脚本
├── Containerfile               # 容器构建文件
├── audio_input/                # 音频输入目录（保留结构）
├── audio_output/               # 字幕输出目录（保留结构）
├── video_input/                # 视频输入目录（保留结构）
├── video_output/               # 视频字幕输出目录（保留结构）
└── *.md                        # 文档文件
```

### ❌ 不应该提交的文件
```
whisper/
├── audio_input/                # 输入音频文件（*.mp3, *.wav等）
├── audio_output/               # 输出字幕文件（*.srt, *.vtt等）
├── video_input/                # 输入视频文件（*.mp4, *.avi等）
├── video_output/               # 输出视频字幕文件
├── whisper_models/             # 下载的模型
├── venv/                       # 虚拟环境
├── __pycache__/                # Python 缓存
├── *.log                       # 日志文件
└── .env                        # 环境变量文件
```

## 🚨 安全注意事项

### 敏感文件排除
- API 密钥文件
- 配置文件中的密码
- 个人音频/视频内容
- 模型微调数据

### 建议做法
1. 使用环境变量存储敏感配置
2. 创建 `.env.example` 模板文件
3. 在文档中说明配置要求
4. 定期审查 `.gitignore` 配置

## 🔄 维护建议

### 定期检查
```bash
# 查看当前被忽略的文件
git status --ignored

# 检查是否有大文件意外提交
git ls-files | xargs ls -lh | sort -k5 -hr | head -10

# 清理已追踪但现在被忽略的文件
git rm --cached filename
```

### 团队协作
1. 确保团队成员了解 `.gitignore` 规则
2. 定期同步 `.gitignore` 更新
3. 在项目文档中说明文件组织规范

## 📋 自定义建议

根据项目需要，您可能需要添加或修改：

```gitignore
# 特定项目需求
custom_models/       # 自定义模型目录
processed_data/      # 处理后的数据
batch_results/       # 批量处理结果

# 特定开发工具
.jupyter/           # Jupyter 配置
.streamlit/         # Streamlit 配置
```

---

💡 **提示**: 这个 `.gitignore` 配置已经覆盖了大多数常见场景。如果您有特殊需求，可以根据实际情况进行调整。